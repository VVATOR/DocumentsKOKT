/// Vikhlaev  
	/// подготовка данных для сохранения
function AllTrim(){
	var div=document.Add.addProduct;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов			
	    //var upperText=div.value;						   // Верхний регистр
	    //    div.value = (upperText.toUpperCase());           
   	
 
		
		
	var div=document.Add.addOwner;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов	
		
	var div=document.Add.addDocType;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов	
	//    var upperText=div.value;						   // Верхний регистр
	//        div.value = (upperText.toUpperCase());    
		
		
	var div=document.Add.addRegNumber;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов	

	var div=document.Add.addOutNumber;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов			
		
	var div=document.Add.addDescription;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов	

	var div=document.Add.addNote;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов	
		//div.value = (div.value).replace(/^\s+|\s+$/g, '');&nbsp;
		
}


function AllTrimEdit(){
	
	var div=document.Edit.editProduct;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов	
	 //   var upperText=div.value;							   // Верхний регистр
     //       div.value = (upperText.toUpperCase());    
	
		
	var div=document.Edit.editOwner;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов	
		
		
	var div=document.Edit.editRegNumber;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов	

	var div=document.Edit.editOutNumber;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов			
    
	var div=document.Edit.editDocType;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов	
	  //  var upperText=div.value;							   // Верхний регистр
      //      div.value = (upperText.toUpperCase());    
	
		
			
	var div=document.Edit.editDescription;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов	

	var div=document.Edit.editNote;
		div.value = (div.value).replace(/^\s+|\s+$/g, ''); // удаление пробелов с права и слева		
		div.value = (div.value).replace(/ {1,}/g," ");	   // удаление двойных пробелов	

}